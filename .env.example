# Elasticsearch vs OpenSearch Benchmarking Configuration
# Copy this file to .env and customize with your actual values

# Elasticsearch Configuration
ELASTICSEARCH_HOSTS=https://your-elasticsearch-cluster.com
ELASTICSEARCH_USER=your-username
ELASTICSEARCH_PASSWORD=your-password

# OpenSearch Configuration
OPENSEARCH_HOSTS=https://your-opensearch-cluster.amazonaws.com

# Index Configuration
INDEX_NAME=sample-companies

# Test Configuration
TEST_DURATION=600        # Test duration in seconds (10 minutes)
NUM_CLIENTS=10          # Number of concurrent clients

# Rally Configuration (optional)
RALLY_LOG_LEVEL=INFO    # Rally logging level: DEBUG, INFO, WARNING, ERROR

# Python Environment
PYTHON_VERSION=3.11.4   # Python version to use with pyenv

# Performance Tuning (optional)
# Uncomment and adjust these values for your specific use case

# HTTP_TIMEOUT=60        # HTTP request timeout in seconds
# CONNECTION_POOL_SIZE=20 # HTTP connection pool size
# MAX_RETRIES=3          # Maximum number of retries for failed requests
# RETRY_BACKOFF=2        # Backoff factor for retries