# Local development cluster configuration
# Copy this to .env for local testing

# Elasticsearch Configuration (local cluster)
ELASTICSEARCH_HOSTS=http://localhost:9200
ELASTICSEARCH_USER=elastic
ELASTICSEARCH_PASSWORD=changeme

# OpenSearch Configuration (local cluster)  
OPENSEARCH_HOSTS=http://localhost:9201

# Index Configuration
INDEX_NAME=test-index

# Test Configuration (shorter for local testing)
TEST_DURATION=30           # 30 seconds for quick tests
NUM_CLIENTS=2             # Lower concurrency for local

# Python Environment
PYTHON_VERSION=3.11.4